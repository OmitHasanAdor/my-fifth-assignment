### 1 What is the difference between var, let, and const?
ans: var is  a function scope variable declaration while let and const is block scope.we can redeclare var but we can't redeclare let and const .we can also reassign var and let's value but can't in const.

### 2 What is the spread operator (...)?
Ans: spread operator is uses to expand array,object like properties.we use it to copy array and object.

### 3 What is the difference between map(), filter(), and forEach()?
Ans: We use forEach to loop any iterable properties.it's doesn't return any array.
while the map() function return a new array.it can change all the element in array and return a new array.filter() uses to make a new array by conditions.it's return a new array.

### 4 What is an arrow function?
Ans: arrow function is a short hand declaration of an function.which is a important upgration from ES6.

### 5  What are template literals?
Ans: template literals is a dynamic method to right strings.we uses backticks in this method.by using this we can easily assign dynamic value